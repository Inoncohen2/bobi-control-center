{
  "name": "bobi-control-center",
  "private": true,
  "version": "1.0.0",
  "description": "מרכז הניהול של בובי — Phase 1, mock data only",
  "license": "MIT",
  "scripts": {
    "setup": "npm --prefix frontend install && python3 -m venv .venv && .venv/bin/pip install -r backend/requirements-dev.txt",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && ../.venv/bin/python -m uvicorn app.main:app --reload --port 8000",
    "build": "npm --prefix frontend run build",
    "test": "npm run test:backend && npm run test:frontend",
    "test:frontend": "npm --prefix frontend run test",
    "test:backend": "cd backend && ../.venv/bin/python -m pytest -q",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:frontend": "npm --prefix frontend run lint",
    "lint:backend": ".venv/bin/ruff check backend/",
    "typecheck": "npm --prefix frontend run typecheck",
    "check": "npm run lint && npm run typecheck && npm run test",
    "docker:build": "docker build -t bobi-control-center:latest .",
    "docker:run": "docker run --rm -p 8000:8000 bobi-control-center:latest"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
